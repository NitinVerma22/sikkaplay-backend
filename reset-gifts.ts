import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { await prisma.gift.deleteMany(); console.log('Gifts deleted!'); } main().catch(e => console.error(e)).finally(() => prisma.$disconnect());
